import React from 'react';

// Logo slider is disabled until logo assets are added.
// Re-enable by providing a logos array and restoring the marquee logic.
const LogoSlider: React.FC = () => null;

export default LogoSlider;
